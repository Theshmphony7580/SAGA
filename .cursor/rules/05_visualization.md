# 📊 Visualization Layer

Handles chart rendering and export.

### Supported Charts
Bar, Line, Scatter, Boxplot, Histogram, Heatmap, Pie

### Rules
- Predict chart type with ML model.
- Render `.html` + `.png`.
- Limit: 20k points max.
