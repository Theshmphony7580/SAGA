# 🚀 DevOps & Deployment

### Containers
- Backend: FastAPI
- Redis, Postgres, Streamlit via docker-compose

### Env Variables
```
OPENAI_API_KEY=your_key
DATABASE_URL=postgresql://user:pass@host/db
DATA_DIR=/data
```

### Cloud Deployment
Render (API), Vercel (UI), Upstash (Redis), Neon (Postgres)

### CI/CD
GitHub Actions → Lint → Test → Build → Deploy
